//import models
var studentModel = require("./students");
var quoteModel = require("./quotes");
var authorModel = require("./authors");
var validationModel = require("./validation");

const models = { studentModel, quoteModel, authorModel, validationModel };

//export models
module.exports = models;
